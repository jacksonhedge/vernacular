node_modules
.next
.env
.env.local
.vercel
.env*.local
station/.env
.aidesigner/*
!.aidesigner/.gitkeep

# iOS: xcconfig files may contain Supabase credentials — keep out of git
# (Commit the template versions once, then ignore further changes)
# VernacularApp/Configs/Debug.xcconfig
# VernacularApp/Configs/Release.xcconfig
