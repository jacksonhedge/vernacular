'use client';

export default function ustationsPage() {
  return (
    <div style={{ flex: 1, display: 'flex', alignItems: 'center', justifyContent: 'center', background: '#f8f9fb' }}>
      <div style={{ textAlign: 'center' }}>
        <div style={{ fontSize: 40, marginBottom: 12 }}>&#x1F6A7;</div>
        <div style={{ fontSize: 20, fontWeight: 700, color: '#0c0f1a', marginBottom: 6 }}>
          ustations
        </div>
        <div style={{ fontSize: 14, color: '#9ca3af' }}>
          This view is being migrated to the new layout.
        </div>
      </div>
    </div>
  );
}
